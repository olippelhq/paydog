name: dogpay_mobile
description: "DogPay - Mobile payment app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # HTTP
  dio: ^5.7.0
  # Secure token storage
  flutter_secure_storage: ^9.2.2
  # State management
  provider: ^6.1.2
  # Navigation
  go_router: ^14.6.2
  # Formatting
  intl: ^0.19.0
  # Observability
  datadog_flutter_plugin: ^3.0.0
  datadog_session_replay: ^1.0.0-preview.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
